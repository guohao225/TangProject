基于SNN聚类的中文命名实体识别主动学习算法
1. 学习器：word2vec+BILSTM+Dense模型
2. 查询策略：当未标注样本经过学习器预测后，假设学习器的输出为[batch_size, max_sequence_length, label_num].我们计算每个句子中含有的实体
如果句子中含有实体，则该句子的向量表示则为句子中所有实体的词向量的平均值。否则句子的向量表示则为每个字的词向量的平均值。
得到每个句子的向量表示后，我们利用SNN对每个句子进行聚类，并将聚类结果通过t-sne降维绘制到散点图中。我们选择那些距离聚类中心最近的样本作为候选样本